name: CI

on:
  push:
    branches: [master, develop]
  pull_request:
    branches: [master, develop]
    types: [opened, reopened, edited]
  workflow_dispatch:

jobs:

  lint:
    uses: ./.github/workflows/lint.yml
    with:
      command: 'lint'
